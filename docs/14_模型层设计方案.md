# 模型层设计方案（LLM：Ollama 本地）

> 版本：V3.0 · 2026-08-17（用户决策：**不使用 Agnes，纯本地模型**）
> 目标：**完全免费** + **本地离线** + **Docker 可打包**
> 关联：docs/15（AI 内容生成服务）、docs/03 模块 1、docs/05（废弃参考）

---

## 一、决策

| 项 | 决策 |
|----|------|
| 模型 | **Ollama 本地 qwen2.5:7b**（零成本、离线可用，用户确认 2026-08-17 弃用 Agnes） |
| 切换方式 | **后端环境变量** `LLM_BASE_URL / LLM_API_KEY / LLM_MODEL`——改配置即换模型，**代码零改动**（llm.service 读取，docs/15 §四） |
| TTS / 渲染 / DB | 维持本地免费方案（Kokoro / Playwright+FFmpeg / MySQL） |
| 模型升级路径 | 本地模型可随时换更大尺寸：`ollama pull qwen2.5:14b`（质量↑ 速度↓，需 ≥16GB 内存）——只改 `.env` 的 `LLM_MODEL` |

## 二、Ollama 配置

```bash
# 本机安装（推荐，Metal 加速）
curl -fsSL https://ollama.com/install.sh | sh   # 或 brew install ollama
ollama pull qwen2.5:7b                           # ~4.7GB
```

后端 `.env` 配置：

```bash
LLM_BASE_URL=http://localhost:11434/v1   # 后端本机跑；后端在 Docker 时用 http://host.docker.internal:11434/v1
LLM_API_KEY=ollama                        # Ollama 不校验 key，任意值
LLM_MODEL=qwen2.5:7b
```

> 已知限制（2026-08-17 实测）：qwen2.5:7b 生成故事词数执行不彻底（目标 8 词常出 2-3 词）、
> JSON 偶发语法错误——已由**候选词注入 + 解析重试 ×3 + 文本英文词自动提取**补偿（docs/15 §三）。
> 换 14b 及以上模型可明显改善。

## 三、Docker 打包说明

| 组件 | 打包方式 | 网络 |
|------|----------|------|
| 后端（本仓库） | 现有 Dockerfile（Hono + MySQL） | 默认网络 |
| MySQL | 现有容器（8.4） | 默认网络 |
| Ollama | 本机安装（Metal 加速，当前）或 `ollama/ollama` 镜像 | `localhost:11434` 或 `http://ollama:11434/v1` |

compose 片段（全容器化时，**随 compose 自动启动**）：

```yaml
  ollama:
    image: ollama/ollama
    restart: unless-stopped      # 随 Docker 自动拉起，挂了自动重启
    volumes:
      - ollama-data:/root/.ollama
    ports:
      - "11434:11434"
volumes:
  ollama-data:
```

> ⚠️ 注意：macOS Docker Desktop 容器内 Ollama **无 Metal GPU 加速**（CPU 跑 7B ≈ 8-12 tok/s）；
> 追求速度时本机装 Ollama（Metal，20-30 tok/s），后端连 `localhost:11434/v1`（本机）或 `host.docker.internal:11434/v1`（后端在 Docker）。

### 3.1 Ollama 启动方式（用户确认 2026-08-17）

| 阶段 | 启动方式 | 手动操作 |
|------|----------|----------|
| **当前（本机开发）** | `nohup ollama serve > ~/ollama.log 2>&1 &`（或另开终端跑 `ollama serve`） | 每次开机后跑一次；也可 `brew services start ollama` 设开机自启，之后不用管 |
| **后期（全容器化）** | `docker compose up -d`，ollama 容器随 compose **自动启动**（`restart: unless-stopped`） | **无需手动开 ollama**，模型存 `ollama-data` volume，pull 一次永久有效 |
| **后期（本机 ollama + 容器其他）** | `brew services start ollama` 开机自启 | 无需手动操作 |

> 决策：用户更在意速度 → 当前用本机 ollama（Metal 加速）；后期全容器化时 ollama 自动随 compose 起，二选一均可（容器内无 Metal，速度约一半，届时按需选择）。

## 四、成本

| 场景 | 动作 | 成本 |
|------|------|------|
| 日常使用 | 本地 qwen2.5:7b | **0 元（永久离线）** |
| 质量不满意 | `ollama pull qwen2.5:14b` 并改 `.env` | 0 元 |
| 需要更强模型 | 可再接入智谱 GLM-4-Flash 免费 / Gemini 免费 tier（同 OpenAI 兼容格式） | 0 元 |

**验收**：`POST /api/content/generate` 跑通，输出质量可接受。

## 五、待办

1. ✅ 用户本机下载 Ollama + qwen2.5:7b（2026-08-17 完成）
2. ✅ 配置 .env 并验证 /api/content/generate 端到端（2026-08-17 完成）
3. ⏳ 按需评估更大模型（qwen2.5:14b）质量/速度
