# AI 英语短视频内容生产平台 — 产品需求文档（PRD）

> 版本：V1.0
> 状态：MVP
> 更新日期：2026-07-28

---

## 一、产品概述

### 1.1 产品定位

AI 英语短视频内容生产平台是一款面向英语学习内容创作者的 AI 辅助生产工具。它的核心目标是：**将选题、文案、词汇整理、配音、视频制作等重复工作流程化，把一条英语学习短视频的制作时间从数小时压缩到分钟级。**

本产品不是英语学习平台，不面向学习者提供课程或练习功能。

### 1.2 目标用户

| 用户类型 | 特征 | 核心诉求 |
|----------|------|----------|
| 英语教育博主 | 在抖音/快手/视频号经营英语学习账号 | 快速批量产出内容，保持账号活跃度 |
| 英语培训机构 | 需要制作教学辅助短视频 | 降低视频制作人力成本 |
| 个人内容创作者 | 想入局英语短视频但缺乏制作能力 | 零门槛生产专业级视频 |

### 1.3 用户痛点

1. **选题难**：每天想新选题，素材枯竭快
2. **写稿慢**：构思中英双语脚本需要英语功底，耗时长
3. **词汇靠猜**：不确定某个词属于四级还是六级，编造不存在的词闹笑话
4. **配音贵**：真人配音成本高、AI 配音需要单独工具
5. **合成烦**：手动用剪辑软件拼文字+图片+音频，操作繁琐

### 1.4 解决方案

用户输入一个主题 → 系统自动完成内容生成（AI）、词汇校验（四六级词库）、配音（TTS）、视频合成（FFmpeg），输出一条可直接发布的 9:16 MP4。

---

## 二、产品目标

### 2.1 MVP 目标

| 维度 | 指标 |
|------|------|
| 功能 | 支持 3 种视频模板，端到端输出可发布 MP4 |
| 质量 | AI 生成内容通过四六级词库校验，词汇准确率 100% |
| 效率 | 单条视频从输入主题到拿到 MP4 ≤ 5 分钟 |
| 可用性 | 单人可独立完成内容生产全流程 |

### 2.2 验证重点

- AI 生成的英语教学内容质量是否满足发布要求
- 视频模板产出在视觉上是否适合持续生产
- 端到端流程是否相比纯人工显著提效

---

## 三、功能需求

### 3.1 视频模板选择（P0）

#### 3.1.1 情景背词（scene_word）

**适用场景**：通过一个短故事阅读，在上下文中学习四六级词汇。

**内容结构**：

| 元素 | 说明 |
|------|------|
| 标题 | 吸引人的视频标题，如"10个四六级词汇藏在一次森林探险里" |
| 情景故事 | 中英混合文本（中文为主体，英文词汇自然嵌入），分段呈现 |
| 词汇高亮 | 故事中的英文词汇在画面上加粗、换色、背景突出 |
| 底部释义 | 当前段落的词汇在画面底部并排展示（单词 + 中文释义） |

**视觉布局**：顶部标题区 → 主体文本区 → 底部词汇提示区（详见《情景词汇阅读视频模板设计规范 V1.0》）

**视频特征**：静态图片 + 文本覆盖 + 词汇高亮 + AI 配音 + 背景音乐

#### 3.1.2 单词卡片（word_card）

**内容结构**：

| 元素 | 说明 |
|------|------|
| 单词 | 英文单词 |
| 词性 | n. / v. / adj. / adv. |
| 释义 | 中文释义 |
| 例句 | 包含该词的英文例句 |
| 例句翻译 | 例句中文翻译（可选） |
| 配图 | 关联图片（可选） |

**视觉特征**：卡片式布局，每张卡片展示一个单词的完整信息，支持多张卡片轮播。

#### 3.1.3 选择题（quiz）

**内容结构**：

| 元素 | 说明 |
|------|------|
| 题干 | 询问词汇含义，如"contract 的意思是？" |
| 选项 | 4 个中文选项（1 正确 + 3 干扰项） |
| 正确答案 | 正确选项索引（0-based） |
| 解析 | 词汇用法解析 |

**视觉特征**：题目面板布局，显示题干和 4 个选项，随后揭晓答案和解析。

### 3.2 AI 内容生成（P0）

#### 3.2.1 输入参数

| 参数 | 类型 | 必填 | 说明 | 默认值 |
|------|------|------|------|--------|
| topic | string | 模板相关 | 视频主题（scene_word 必填，word_card/quiz 可选） | — |
| template | string | 是 | 模板类型：scene_word / word_card / quiz | — |
| level | string | 是 | 四六级等级：CET4 / CET6 | — |
| wordCount | number | 否 | 词汇数量，范围 3~15 | 10 |
| targetDuration | number | 否 | 目标视频时长（秒） | scene_word:60, word_card:45, quiz:30 |

#### 3.2.2 生成过程

```
scene_word 模板：
  ① LLM 根据 topic + level 生成中英混合故事（title + segments）
  ② code 提取候选英文词汇（去重）
  ③ HTTP 请求四六级词库验证每个词的等级和释义
  ④ code 只保留匹配成功的词汇，丢弃 AI 编造的词
  ⑤ code 组装为 partial_dto（status="content_ready"）

word_card 模板：
  ① HTTP 请求四六级词库随机抽取 wordCount 个词
  ② LLM 为每个词生成卡片内容（词性、释义、例句、例句翻译）
  ③ code 组装为 partial_dto

quiz 模板：
  ① HTTP 请求四六级词库随机抽取 wordCount 个词
  ② LLM 为每个词生成选择题（题干 + 4 选项 + 正确答案 + 解析）
  ③ code 组装为 partial_dto
```

### 3.3 内容编辑（P0）

用户在 AI 生成后可进行人工修改：

| 可操作项 | 说明 |
|----------|------|
| 修改标题 | 直接编辑标题文本 |
| 修改正文 | 编辑情景故事文本 / 单词卡片 / 题目 |
| 调整词汇 | 替换、增删重点词汇 |
| 重新生成 | 不满意则重新触发 AI 生成 |

说明：编辑操作通过 `EditContentRequest` DTO 提交，核心字段：title, content, words, voice, style。

### 3.4 四六级词汇处理（P0）

#### 3.4.1 词库数据

| 字段 | 说明 |
|------|------|
| word | 英文单词 |
| level | 四级 (CET4) / 六级 (CET6) |
| meaning | 中文释义 |
| frequency | 词频（用于排序和筛选） |

#### 3.4.2 处理规则

- 词汇等级由词库决定，AI 不参与等级判定
- AI 负责语义分析和内容生成，词库保证词汇准确性
- AI 输出的候选词必须经过词库校验，匹配失败的直接丢弃
- MVP 阶段不设人工审核，词汇校验自动完成

#### 3.4.3 API 接口

| 接口 | 用途 |
|------|------|
| POST /api/cet/validate-words | 验证候选词列表是否在四六级词库中 |
| POST /api/cet/random-words | 按等级随机抽取指定数量的词汇（word_card / quiz 使用） |

### 3.5 AI 配音（P0）

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 音色 | 可选 female_01 / male_01 等 | female_01 |
| 语速 | 0.5 ~ 2.0 | 1.0 |

- 调用外部 TTS 服务，输入文本 + 音色配置，返回音频文件
- 音频格式：mp3
- 后端根据模板类型自动拼接朗读文本（如 scene_word 将 segments 拼接为完整朗读文本）

### 3.6 视频生成（P0）

#### 3.6.1 输出规格

| 属性 | 规格 |
|------|------|
| 比例 | 9:16 |
| 分辨率 | 1080 × 1920 |
| 格式 | MP4 |
| 内容 | 背景图片 + 文本渲染 + 词汇高亮 + AI 配音 + 背景音乐 |

#### 3.6.2 渲染流程

```
ContentDTO + 音频文件 + 背景音乐
          │
          ▼
  HTML 模板渲染（根据 template 选择对应模板，注入 content + style）
          │
          ▼
  生成图片帧（Puppeteer / Playwright 截图）
          │
          ▼
  FFmpeg 合成（图片帧 + audio + BGM → MP4）
          │
          ▼
      上传 CDN，返回视频 URL
```

### 3.7 内容记录（P0）

系统保存每次生产的完整 ContentDTO 到数据库，保留字段包括：标题、模板类型、文本内容、词汇信息、音频地址、视频地址、生成时间等，供后续复盘、二次编辑和数据分析使用。

### 3.8 样式配置（P1）

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| background | 背景主题 ID | 依模板而定 |
| font | 字体 | default |
| colorScheme | 配色方案 | 依模板而定 |
| bgm | 背景音乐曲目 ID | 依模板而定 |

---

## 四、MVP 不包含功能

以下功能明确排除在 MVP 之外：

- ❌ 用户系统（注册/登录/权限）
- ❌ 社区功能（评论/点赞/分享）
- ❌ 学习打卡
- ❌ 复杂数据分析
- ❌ 自动发布抖音
- ❌ 视频动画效果
- ❌ 多人协作
- ❌ 音色定制/上传
- ❌ 自定义视频模板

---

## 五、用户流程

### 5.1 核心用户路径

```
1. 进入平台
      │
      ▼
2. 选择视频模板（情景背词 / 单词卡片 / 选择题）
      │
      ▼
3. 填写参数（主题、等级、词汇数量、时长）
      │
      ▼
4. 点击"生成内容"
      │
      ▼
5. AI 生成内容 + 词库自动校验（约 10~30 秒）
      │
      ▼
6. 预览内容，确认或编辑修改
      │
      ▼
7. 选择音色和背景样式
      │
      ▼
8. 点击"生成视频"
      │
      ▼
9. 系统 TTS 配音 + 视频渲染（约 1~3 分钟）
      │
      ▼
10. 下载 MP4 或预览播放
```

### 5.2 MVP 简化说明

- 步骤 5 和 7 在 MVP 中自动串联（内容生成完成后自动进入媒体生产），不阻塞等待用户确认
- 步骤 6 的编辑为可选操作，用户可跳过直接进入视频生成
- 后续接入人工审核时，在步骤 5 和步骤 8 之间插入审核节点，流程无需大改

---

## 六、数据结构（ContentDTO）

### 6.1 设计原则

- 所有模板共享同一套核心数据模型
- 内容差异化通过 TypeScript discriminated union 实现
- DTO 分四层：Request（输入）→ ContentDTO（流转）→ Response（输出）→ DB Entity（持久化）
- 新增模板只需加一个 ContentItem 子类型 + 一个 Renderer，核心结构不变

### 6.2 顶层字段

```typescript
interface ContentDTO {
  id: string;                    // cnt_YYYYMMDD_XXXXXX
  template: TemplateType;        // "scene_word" | "word_card" | "quiz"
  title: string;
  level: CefrLevel;              // "CET4" | "CET6"
  targetDuration: number;        // 目标时长（秒）
  content: ContentArray;         // discriminated union
  words: WordInfo[];             // 去重后的词汇汇总
  style: StyleConfig;
  voice: VoiceConfig;
  audio?: AudioInfo;             // TTS 后填充
  video?: VideoInfo;             // 渲染后填充
  status: ContentStatus;
  createdAt: string;             // ISO 8601 UTC
  updatedAt: string;
}
```

### 6.3 状态流转

```
draft → ai_generating → content_ready → tts_processing → audio_ready
                                                    → video_rendering → completed
（任一阶段异常触发 → failed）
```

完整字段定义见 `04_Content_DTO设计文档.txt`。

---

## 七、系统架构

### 7.1 概述

```
┌─────────────────────────────────────────┐
│                后端服务层（LLM 直连）       │
│                                          │
│  ┌─────────────────────────────────┐    │
│  │   Workflow A：内容生成（三个模板）  │    │
│  │   LLM 生成 → 词库校验 → 组装 DTO   │    │
│  │   输出：partial_dto               │    │
│  └──────────────┬──────────────────┘    │
│                 │                       │
│  ┌──────────────┴──────────────────┐    │
│  │   Workflow B：媒体生产（共用）     │    │
│  │   TTS 配音 → 视频渲染             │    │
│  │   输出：final_dto                 │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
                 │
                 ▼
    ┌─────────────────────────┐
    │     后端 API 服务         │
    │  · 四六级词库服务          │
    │  · TTS 服务               │
    │  · 视频渲染服务            │
    └─────────────────────────┘
```

### 7.2 外部依赖

| 服务 | 接口 | 用途 |
|------|------|------|
| 四六级词库 | POST /api/cet/validate-words | 验证候选词 |
| 四六级词库 | POST /api/cet/random-words | 随机抽取词汇 |
| TTS 服务 | POST /api/tts/generate | 文本转语音 |
| 视频渲染服务 | POST /api/video/render | HTML 渲染 + FFmpeg 合成 |
| LLM | GPT-4o / Claude 3.5 Sonnet API | 内容生成 |

### 7.3 技术栈

| 层 | 技术选型 |
|----|----------|
| 流程编排 | 无（后端 service 直连 LLM，2026-08-17 去 Dify） |
| 后端语言 | TypeScript |
| AI 模型 | GPT-4o / Claude 3.5 Sonnet（推荐支持 Structured Output） |
| 视频渲染 | HTML 模板 + Playwright/Puppeteer + FFmpeg |
| 数据存储 | 关系型数据库（具体选型待定） |

---

## 八、非功能需求

### 8.1 性能

| 指标 | 目标 |
|------|------|
| 内容生成耗时 | ≤ 30 秒 |
| TTS 合成耗时 | ≤ 60 秒 |
| 视频渲染耗时 | ≤ 120 秒 |
| 端到端总耗时 | ≤ 5 分钟 |

### 8.2 可靠性

- 词汇校验必须通过独立词库，不允许 AI 自行创造四六级词汇
- LLM 生成内容中的候选词匹配失败时直接丢弃，保证词汇准确性 100%
- 任一阶段异常时状态标记为 failed，不产出半成品

### 8.3 可扩展性

- 新增模板只需：定义 ContentItem → 加入联合类型 → 实现 Renderer
- 核心 ContentDTO 和 Workflow 无需改动
- 人工审核环节可在 Workflow A 和 B 之间插入，不影响下游

### 8.4 兼容性

- 输出视频兼容抖音、快手、视频号
- 格式：9:16、1080×1920、MP4

---

## 九、成功标准

| 标准 | 衡量方式 |
|------|----------|
| 可稳定生成可发布视频 | 连续 10 次生产成功率 ≥ 95% |
| 内容质量达标 | 人工抽检 20 条，内容自然度 ≥ 4/5 |
| 词汇准确性 | 词库校验通过率 100%（AI 编造词全部丢弃） |
| 制作效率提升 | 单条视频从输入到 MP4 ≤ 5 分钟 |

---

## 十、后续规划

| 阶段 | 内容 |
|------|------|
| 短期（MVP 后） | 接入人工审核环节，支持编辑后重生成 |
| 中期 | 批量生产（一次输入多个主题）、内容资产管理 |
| 长期 | 数据分析看板、词汇管理系统、多模板市场、平台化运营 |

---

## 十一、附录

### 11.1 术语表

| 术语 | 全称 | 说明 |
|------|------|------|
| DTO | Data Transfer Object | 数据在各层之间流转的结构定义 |
| ContentDTO | — | 本项目统一的视频内容数据载体 |
| Dify（已废弃） | — | 原 AI 编排平台，2026-08-17 起由后端直连 LLM 取代 |
| TTS | Text-to-Speech | 文本转语音服务 |
| CET4/CET6 | College English Test | 大学英语四六级考试 |
| discriminated union | — | TypeScript 技巧：通过一个共有字段（如 template）区分不同子类型 |
| partial_dto | — | Workflow A 输出的 ContentDTO 半成品（缺 audio/video） |
| final_dto | — | Workflow B 输出的完整 ContentDTO（含 audio/video） |

### 11.2 参考文档

| 文档 | 内容 |
|------|------|
| 01_项目概述文档.txt | 项目定位、核心流程、发展方向 |
| 02_MVP需求文档.txt | MVP 功能需求与成功标准 |
| 03_系统模块设计文档.txt | 模块划分 |
| 04_Content_DTO设计文档.txt | 统一数据结构定义 |
| 05_Dify_Workflow设计文档.txt（废弃） | Dify Workflow 节点详细设计（已被 docs/15 取代） |
| 06_视频生产SOP文档.txt | 选题→生成→审核→制作→发布 SOP |
| 07_后续扩展规划文档.txt | 中远期功能规划 |
| 情景词汇阅读视频模板设计规范 V1.0.txt | scene_word 模板视觉/内容规范 |
